<h3>WhatsOn</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Title"], "WhatsOn/detail", new { userKey = item["UserKey"] }, new { @class = "title" })
        </li>
      }
</ul>