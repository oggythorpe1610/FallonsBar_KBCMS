<h3>Home_Slider</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Img"], "Home_Slider/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>