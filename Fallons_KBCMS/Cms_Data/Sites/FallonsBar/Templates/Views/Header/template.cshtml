 	     <div class="wrap">
			<div class="top-header">
				@*<div class="logo">
					<a href="@Url.FrontUrl().PageUrl("Home")"><img src="@Url.FrontUrl().FileUrl("images/logo.png")" title="logo" /></a>
				</div>*@
                <div class="logotext">
                    <a href="@Url.FrontUrl().PageUrl("Home")"><p>fallon's bar &amp; brasserie</p></a>
                </div>
				<div class="clear"> </div>
			</div>
			<!---start-top-nav---->
			<div class="top-nav">
				<div class="top-nav-left">
          <ul>
 
    @foreach (var item in MenuHelper.Top())
   {
        if (item.Navigation.Show)
       {
          <li class ='@(MenuHelper.IsCurrent(item) ? "active" : "")'> @Html.FrontHtml().PageLink(item.LinkText, item.FullName)</li>
       }
   }
     </ul>
				</div>
				<div class="top-nav-right">
					<div class="telephone">
						<span><i class="phone_img"><img src="@Url.FrontUrl().FileUrl("images/phone.png")" alt="" /></i> @("CALL".Label())</span><span class="number">@("(800) 234-5678".Label())</span>
					</div>
				</div>
				<div class="clear"> </div>
			</div>
			<!---End-top-nav---->
		</div>